import {redirect} from "next/navigation";import {createClient} from "@/lib/supabase/server";import {Logo} from "@/components/logo";import {login} from "./actions";
export default async function Login({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const s=await createClient();const{data}=await s.auth.getClaims();if(data?.claims?.sub)redirect("/admin");const p=await searchParams;const e=Array.isArray(p.error)?p.error[0]:p.error;return <main className="auth"><div className="auth-card"><Logo/><h1>Acceso administrativo</h1><p>Repositorio de Recargas de Energía Eléctrica</p>{e&&<div className="error">{e}</div>}<form action={login}><label>Correo<input type="email" name="email" required/></label><label>Contraseña<input type="password" name="password" required/></label><button>Ingresar</button></form><a href="/">← Volver al portal</a></div></main>}
